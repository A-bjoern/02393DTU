//
// Created by asbpo on 03-09-2024.
//

#include "compareNumbers.h++"
#include <iostream>
/*
int main() {
    int x,y;
    std::cin >> x >> y;
    if(x > y) {
        std::cout << "x is greater than y";
        return 0;
    }else if(x < y){
     std::cout << "x is smaller than y";
     return 0;
    }
    std::cout << "x is equal to y";
    return 0;
};
 */