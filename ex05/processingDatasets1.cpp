//
// Created by asbpo on 23-09-2024.
//

#include <vector>
#include <iostream>
#include <algorithm>

/*
int main() {
    std::vector<int> a, b;
    int res = 0;
    std::string txt;
    std::string val;
    while(true) {
        std::cin >> txt >> val;
        if(std::cin.fail()) break;
        if(txt == "a") a.push_back(stoi(val));
        else b.push_back(stoi(val));
    }
    while(a.size() < b.size()){
        a.push_back(0);
    }
    while(b.size() < a.size()){
        b.push_back(0);
    }
    for(int i = 0; i < a.size(); i++){
        res += a.at(i)*b.at(i);
    }
    std::cout << res << std::endl;
    return 0;
}
 */